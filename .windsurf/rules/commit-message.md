# Commit message style

- Three short lines (<= 80 chars each)
- Format: `<type>: <summary>` (e.g. `fix: handle null user IDs`)
- Use concrete verbs: add, fix, remove, refactor, docs
- No fluff: avoid "improve", "various updates", "refactor codebase"
